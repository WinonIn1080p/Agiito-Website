document.addEventListener('DOMContentLoaded', function () {
    const container = document.querySelector('.homepage-body .scroll-container');
    container.scrollTop = 0; // Ensure we start at the top of the container
});
